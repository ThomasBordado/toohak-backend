import { messageInput } from "./interfaces"

export const getSessionResult = (playerId: number) => {

}

export const getSessionMessages = (playerId: number) => {

}

export const sendSessionMessage = (playerId: number, message: messageInput) => {

}